version: "2"
linters:
  default: standard
  disable:
    - staticcheck
  settings:
    errcheck:
      exclude-functions:
        - fmt.Fprintf
        - fmt.Fprint
        - fmt.Fprintln
        - (io.Closer).Close
        - (*os.File).Close
        - (*io.PipeWriter).Close
        - (*crypto/tls.Conn).Close
        - (net.Conn).Close
        - (net.Listener).Close
        - (io.WriteCloser).Close
