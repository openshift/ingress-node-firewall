version: "2"
linters:
  default: standard
  settings:
    errcheck:
      exclude-functions:
        - fmt.Fprintf
        - fmt.Fprintln
        - (io.Closer).Close
        - (*os.File).Close
        - (net.Conn).Close
        - (net.Listener).Close
        - (*io.PipeWriter).Close
        - (*crypto/tls.Conn).Close
  exclusions:
    rules:
      - linters:
          - staticcheck
        text: "SA1019: scheme.Builder is deprecated"
      - linters:
          - staticcheck
        text: "ST1001"
        path: "test/"
      - linters:
          - staticcheck
        text: "QF1001"
      - linters:
          - staticcheck
        text: "QF1003"
      - linters:
          - staticcheck
        text: "QF1008"
