version: "2"

linters:
  default: standard
  settings:
    errcheck:
      exclude-functions:
        - fmt.Fprintf
        - fmt.Fprintln
        - fmt.Fprint
        - (io.Closer).Close
        - (io.WriteCloser).Close
        - (io.ReadCloser).Close
        - (*os.File).Close
        - (*net.TCPListener).Close
        - (net.Conn).Close
        - (net.Listener).Close
        - (*crypto/tls.Conn).Close
        - (*io.PipeWriter).Close
        - (*bufio.Writer).Flush
    staticcheck:
      checks:
        - "all"
        - "-ST1000"
        - "-ST1001"
        - "-ST1003"
        - "-ST1020"
        - "-ST1021"
        - "-QF1001"
        - "-QF1003"
        - "-QF1008"
